<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Книга рецептов — Кухня</title>
  <link rel="manifest" href="/manifest.json" />
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="topbar">
    <h1>Рецепты</h1>
    <div class="actions">
      <button id="importBtn" title="Импортировать JSON">Импорт</button>
      <button id="exportBtn" title="Экспортировать JSON">Экспорт</button>
      <button id="toggleEditBtn" title="Режим редактирования">Редактировать</button>
    </div>
  </header>

  <main class="layout">
    <aside class="list" id="recipeList"></aside>
    <section class="detail" id="detail">
      <p class="hint">Выберите рецепт слева</p>
    </section>
  </main>

  <!-- hidden file input for import -->
  <input type="file" id="fileInput" accept=".json" style="display:none" />

  <template id="recipeItemTpl">
    <div class="recipe-item">
      <div class="title"></div>
      <div class="meta"></div>
    </div>
  </template>

  <script src="/app.js"></script>
</body>
</html>
