:root{
  --bg:#ffffff; --text:#121212; --muted:#666; --accent:#0a84ff;
  --card:#fafafa; --radius:10px;
  font-family:system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
}
*{box-sizing:border-box}
body{margin:0;background:var(--bg);color:var(--text);font-size:16px}
.topbar{display:flex;align-items:center;justify-content:space-between;padding:12px 16px;border-bottom:1px solid #eee;position:sticky;top:0;background:var(--bg)}
.topbar h1{margin:0;font-size:18px}
.actions button{margin-left:8px;padding:8px 10px;border-radius:8px;border:1px solid #eee;background:transparent;cursor:pointer}
.layout{display:flex;height:calc(100vh - 64px)}
.list{width:320px;border-right:1px solid #eee;padding:12px;overflow:auto;background:var(--card)}
.detail{flex:1;padding:18px;overflow:auto}
.recipe-item{padding:10px;border-radius:8px;margin-bottom:8px;cursor:pointer;border:1px solid transparent}
.recipe-item:hover{background:#fff;box-shadow:0 1px 4px rgba(0,0,0,.03)}
.recipe-item .title{font-weight:600}
.recipe-item .meta{font-size:13px;color:var(--muted)}
.card{background:#fff;border-radius:12px;padding:14px;box-shadow:0 6px 18px rgba(0,0,0,.04)}
.section-title{font-weight:700;margin-top:8px;margin-bottom:6px}
.ingredients{margin:0;padding-left:20px}
.method{white-space:pre-wrap;font-family:inherit}
.controls{margin-top:12px;display:flex;gap:8px}
.small{font-size:13px;color:var(--muted)}
img.recipe-photo{max-width:100%;border-radius:8px;margin-top:10px}
.editable{outline:2px dashed rgba(10,132,255,.12);padding:8px;border-radius:8px}
@media (max-width:760px){
  .layout{flex-direction:column}
  .list{width:100%;height:160px;order:2;border-right:none;border-top:1px solid #eee}
  .detail{order:1}
}
